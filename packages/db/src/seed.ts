// import { seed } from "drizzle-seed"

// import { db } from "./client"
// import { users } from "./schemas"

// currently disabled, since `drizzle-seed` seems not to work with RQB v2
async function main() {
  //await seed(db, { users }, { count: 10 })
}

void main()
